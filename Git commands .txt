git init

git add .             /* for inserting all documents*/

git status

git commit -m "First commit"

git branch -M main 

git remote add origin https://github.com/Subin-Kumar/FuturaSubin.git

git push -u origin main



THEN ONWARDS:

git add .

git commit -m "second change"

git push


/*Add .gitignore*/

touch .gitignore         /*in gitbash ->creates the file*/

node_modules/
client/node_modules/
server/node_modules/    /*write in .gitignore file*/




